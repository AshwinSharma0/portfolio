Place your resume PDF in this folder as `resume.pdf` so the site serves it at `/resume.pdf`.

Steps:
1. Add your PDF file and name it `resume.pdf`.
2. Rebuild / redeploy the site if necessary (e.g., `npm run build`, then `npm run preview` or redeploy on Vercel).

Access URL: https://<your-domain>/resume.pdf
